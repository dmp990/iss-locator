import React from "react";

import icon from "../assets/satellite.png";

const RenderSpaceStation = () => {
  return (
    <div>
      <img src={icon} width={100} height={100}></img>
    </div>
  );
};

export default RenderSpaceStation;
